as of today, my intial implementation on 3rd/feb/25


First one is my server response times; Nothing much it just returns Ok http response.
Without any request handling at all.

ajreddy@CPC-ajred-PC7GV:/mnt/q/source/AjayServer/src$ wrk -H 'Host: localhost' -H 'Accept: text/plain,text/html;q=0.9,application/xhtml+xml;q=0.9,application/xml;q=0.8,*/*;q=0.7' -H 'Connection: keep-alive' --latency -d 15 -c 512 --timeout 8 -t 4 http://localhost:8080
Running 15s test @ http://localhost:8080
  4 threads and 512 connections
  Thread Stats   Avg      Stdev     Max   +/- Stdev
    Latency    44.23ms   12.41ms 148.33ms   71.91%
    Req/Sec     1.32k   229.65     1.94k    66.78%
  Latency Distribution
     50%   42.74ms
     75%   51.44ms
     90%   59.98ms
     99%   76.61ms
  78535 requests in 15.08s, 5.77MB read
  Socket errors: connect 0, read 78529, write 781688, timeout 0
Requests/sec:   5207.24
Transfer/sec:    391.56KB

This is fasthttp helloWorldServer
Without any request handling or config or whatsoever

ajreddy@CPC-ajred-PC7GV:/mnt/q/source/AjayServer/src$ wrk -H 'Host: localhost' -H 'Accept: text/plain,text/html;q=0.9,application/xhtml+xml;q=0.9,application/xml;q=0.8,*/*;q=0.7' -H 'Connection: keep-alive' --latency -d 15 -c 512 --timeout 8 -t 4 http://localhost:8080
Running 15s test @ http://localhost:8080
  4 threads and 512 connections
  Thread Stats   Avg      Stdev     Max   +/- Stdev
    Latency     6.35ms    5.25ms 223.41ms   81.91%
    Req/Sec    17.83k     3.11k   27.12k    69.83%
  Latency Distribution
     50%    5.17ms
     75%    8.78ms
     90%   12.56ms
     99%   21.22ms
  1060331 requests in 15.09s, 150.67MB read
  Socket errors: connect 0, read 0, write 32, timeout 0
Requests/sec:  70273.45
Transfer/sec:      9.99MB
ajreddy@CPC-ajred-PC7GV:/mnt/q/source/AjayServer/src$


I am as of now 7.33(44/6) times slower than fasthttp library.
    1. I yet don't process any customer config or http headers; no logic of any kind inside request handling
    2. I am getting error for few requests as EOF while reaing from connection, need to fix it.

5th/feb/2025

Latency is about same as i logged before.
Implemented multi process approach and also implemented go routine pool instead of creating single go routine for reach request
1. I need to look at non blocking read calls while reading from connections
2. also while reading i am creating buffer everytime, may be i can reuse it within a go routine??
3. I think i can add support for http2 or quic, as i don't have any USP other than speed(if I achieve this)

6th/feb
Identified that child processes are not terminating when parent process terminated. Fixed that issue
used buffer pool instead of creating buffer every time we handle a request, didn't make much difference
Also the eof errors are may be just due to client abruptly closing connections, i've added error handling for the eof error
Perf is still around the same numbers between 45-55ms/sec

todo: I still need to work on non-blocking read calls while reading from connections i.e use IOCP

I tried IOCP while accepting connections, but it seems very difficult. Seems like we don't have proper support
  update: go sever already uses the iocp underneath the hood
for iocp in windows go packages. I am not able to get file handle for socket connection. And the only way to do it
is to use pure windows networking APIs instead of using go network packages. Seems like not worth the trouble.

Either i need to switch to linux for developing this or move on the parsing http request and providing other functionality.

I see that i when i add more than -c 100 in the wrk tool, i am exceeding time around 7ms. i.e i need to optimize for concurrency
I see cpu is maxing out in test, i need to use profiler for go and see how to optimize it.
I see a tool called pprof which can give be a profiler for my go app. Let's work on it next.

update: I decreased the time, the issue is the wrk is creating long lived tcp connections i.e it is 
using single tcp connection to send multiple http requests, where as i am not handling it properly. After 
fixing that upto 200 simultaneous connections i was able to handle at 1ms/request.
But still i faced write errors when i use 400 connections.
Turns out issue is, now the connections are long lived and i am using a go routine pool of 32, i can only process
32 connections, since handleConnection for each connection only returns at the end of the test. Now i increased go routine pool coun to 600
Now i have comparable perf to fasthttp.

wrk -H 'Host: localhost' -H 'Accept: text/plain,text/html;q=0.9,application/xhtml+xml;q=0.9,application/xml;q=0.8,*/*;q=0.7' -H 'Connection: keep-alive' --latency -d 15 -c 512 --timeout 8 -t 4 http://localhost:8080
Running 15s test @ http://localhost:8080
  4 threads and 512 connections
  Thread Stats   Avg      Stdev     Max   +/- Stdev
    Latency     8.16ms   25.01ms 633.33ms   98.87%
    Req/Sec    18.96k     3.73k   35.32k    70.17%
  Latency Distribution
     50%    4.75ms
     75%    8.43ms
     90%   13.13ms
     99%   40.80ms
  1129370 requests in 15.10s, 82.93MB read
  Socket errors: connect 0, read 0, write 471, timeout 0
Requests/sec:  74811.91
Transfer/sec:      5.49MB

From perf profiles i can still see that the conn.Write call is taking lot of time but not sure. How to solve that issue? I can see online that c calls from go is expected tot ake that much time
I can compare this with fast server profile later. If that also has similar issue then it is platform issue, nothing much that i can do.

For now i will move my focus to parsing the http request and sending the proper response.


After parsing the first line of http request:
ajreddy@CPC-ajred-NFF4D:/mnt/c/Users/ajreddy$ wrk -H 'Host: localhost' -H 'Accept: text/plain,text/html;q=0.9,application/xhtml+xml;q=0.9,application/xml;q=0.8,*/*;q=0.7' -H 'Connection: keep-alive' --latency -d 15 -c 512 --timeout 8 -t 4 http://localhost:8080
Running 15s test @ http://localhost:8080
  4 threads and 512 connections
  Thread Stats   Avg      Stdev     Max   +/- Stdev
    Latency     6.61ms    6.70ms 247.41ms   89.48%
    Req/Sec    17.94k     3.79k   31.20k    70.36%
  Latency Distribution
     50%    4.96ms
     75%    8.90ms
     90%   13.57ms
     99%   25.59ms
  1062325 requests in 15.09s, 78.01MB read
Requests/sec:  70421.38
Transfer/sec:      5.17MB
Exactly not sure, why it came down. I believe it's due to the fact that i redirected output to file. 
One more point to note is that, I am still seeing few write errors like around ~100 range. This even i saw for fasthttp with my machine. So will don't consider it for now.

Next task is to parse http request. I will understand the support from fastHttp and see what methods i should support for parsing and writing response.
After that i will laydown my highlevel classes and interfaces for the same. Then i need to code.


I've defined the types for parsing and setting http request and response. 
Also looked at different type of request methods and request compressions.
I still need to figure out byte[] vs string and custom buffers vs strings to pass around request and response. Would it effect perf that much or i can rewrite parts of it later?

From the list that fast http handles, I still needs to add some features. 

TLS support [todo]
Streaming & Large File Handling
Supports limiting concurrent requests to prevent overload.
Can manually configure CORS headers for cross-origin requests.
Supports easy JSON encoding/decoding with Go’s encoding/json. [done]
added code to read the request content and parse for form url encode and multipart form data and json content types [done]
need to check perf after doing this[todo]
Need to add interface support defined in TypesToAdd.txt[todo]
  1. Add support to add and delete cookies
  2. Add support to read properties from request
  3. Add support to form response and send
